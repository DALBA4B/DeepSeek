# DeepSeek Telegram Bot 🤖

Групповой Telegram бот на Python, который использует DeepSeek AI для интеллектуальных ответов. Бот читает сообщения, анализирует контекст и отвечает естественным образом, используя различные форматы ответов (текст, реакции, гифки, стикеры).

## Особенности

- ✅ **AI-powered responses** — использует DeepSeek API для генерации ответов
- ✅ **Multiple response formats** — текст, реакции emoji, гифки, стикеры
- ✅ **Smart memory system** — краткосрочная (RAM) и долгосрочная (Firebase) память
- ✅ **Natural conversation** — 10% случайный ответ, реагирует на упоминания и вопросы
- ✅ **Secure credentials** — все секреты хранятся в `.env` файле
- ✅ **Modular architecture** — чистый, легко читаемый код

## Требования

- Python 3.8+
- Telegram Bot Account (получи token у [@BotFather](https://t.me/botfather))
- DeepSeek API Key (от https://api.deepseek.com)
- Giphy API Key (от https://developers.giphy.com)
- Firebase Firestore (с credentials JSON файлом)

## Быстрый старт

### 1. Клонировать/Загрузить проект

```bash
cd DeepSeek
```

### 2. Создать виртуальное окружение (опционально но рекомендуется)

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить переменные окружения

Скопируй `.env.example` в `.env` и заполни реальные значения:

```bash
cp .env.example .env
```

Отредактируй `.env` файл со своими ключами:

```env
# Telegram Bot
TELEGRAM_TOKEN=your_telegram_token_here

# DeepSeek API
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Giphy API
GIPHY_API_KEY=your_giphy_api_key_here

# Firebase
FIREBASE_CRED_PATH=path_to_firebase_credentials.json

# Bot Settings
BOT_NAME=YourBotName
CHAT_ID=  # Опционально: ID группы для ограничения

# Logging
LOG_LEVEL=INFO
```

### 5. Запустить бота

```bash
python main.py
```

Бот начнет опрашивать Telegram API и будет готов к получению сообщений.

## Структура проекта

```
.
├── .env                          # Переменные окружения (НЕ коммитить!)
├── .env.example                  # Шаблон для .env
├── .gitignore                    # Git ignore (защищает .env)
├── config.py                     # Загрузка конфигурации из .env
├── main.py                       # Точка входа, обработчик сообщений
├── memory.py                     # Двухуровневая память (RAM + Firebase)
├── brain.py                      # AI логика и генерация ответов (DeepSeek)
├── responder.py                  # Отправка разных типов ответов
├── requirements.txt              # Зависимости Python
└── README.md                     # Этот файл
```

## Архитектура

### Поток данных

```
Telegram Message
    ↓
handle_message() — проверка и фильтрация
    ↓
Memory.add_message() — сохранение в память
    ↓
Brain.should_respond() — проверка условий ответа
    ↓
Brain.generate_response() — запрос к DeepSeek AI
    ↓
Responder.parse_response() — определение типа ответа
    ↓
Responder.send_*() — отправка в Telegram
```

### Компоненты

#### `config.py`
Загружает переменные из `.env` файла с валидацией. Гарантирует что все нужные секреты присутствуют.

#### `memory.py` — Class Memory
**Краткосрочная память (Python список):**
- Последние 30 сообщений в RAM
- Быстрый доступ для контекста

**Долгосрочная память (Firebase Firestore):**
- Коллекция `messages`: {user_id, username, text, timestamp, message_id}
- Коллекция `users`: {username, last_seen}
- Постоянное хранилище

#### `brain.py` — Class Brain
**Логика когда отвечать:**
1. Упоминание имени бота (регистр-независимо)
2. Наличие вопросительного знака `?`
3. 10% случайная вероятность на любое сообщение

**Генерация ответов:**
- Использует OpenAI-совместимый клиент для DeepSeek API
- Системный промпт определяет личность бота
- Передает контекст (последние 20 сообщений)

#### `responder.py` — Class Responder
Четыре формата ответа:
1. **TEXT** — обычное текстовое сообщение
2. **REACT:😂** — реакция emoji на сообщение
3. **GIPHY:query** — гифка из Giphy API
4. **STICKER:emotion** — стикер из предустановленного пака

Все методы имеют fallback-стратегии при ошибках API.

## Безопасность

### Переменные окружения

⚠️ **НИКОГДА не коммитьте `.env` файл с реальными секретами!**

`.gitignore` уже настроен чтобы автоматически исключить `.env` файлы из git.

Используй `.env.example` как шаблон для других разработчиков:
```bash
cp .env.example .env.your_username  # Личный .env
```

### Защита учетных данных

- ✅ Все токены и ключи в `.env` (не в коде)
- ✅ Firebase credentials хранятся в отдельном JSON файле
- ✅ `.gitignore` защищает конфиденциальные файлы
- ✅ `config.py` валидирует наличие всех необходимых переменных

## Логирование

Бот логирует все важные события:

```
2026-01-24 11:35:07,623 - __main__ - INFO - Bot is running... Press Ctrl+C to stop
2026-01-24 11:35:21,245 - memory - INFO - Message saved - John: какая погода сегодня?
2026-01-24 11:35:21,250 - brain - INFO - Should respond: question mark in message
2026-01-24 11:35:22,100 - brain - INFO - Generated response: похоже будет дождик
```

Уровень логирования настраивается переменной `LOG_LEVEL` в `.env`:
- `DEBUG` — все события
- `INFO` — важные события (по умолчанию)
- `WARNING` — только ошибки и предупреждения
- `ERROR` — только ошибки

## Ошибки и решения

### "TELEGRAM_TOKEN not found in environment variables"

Убедись что:
1. Файл `.env` существует в корне проекта
2. В `.env` заполнено значение `TELEGRAM_TOKEN=...`
3. Спецсимволы в токене не экранированы

### "Cannot reach Telegram API"

- Проверь интернет соединение
- Проверь что token правильный (скопируй снова из BotFather)
- Убедись что бот добавлен в группу и имеет права

### "DeepSeek API Error"

- Проверь баланс DeepSeek аккаунта
- Убедись что API key правильный
- Проверь интернет соединение

## Развитие и кастомизация

### Изменение личности бота

В `brain.py` отредактируй `SYSTEM_PROMPT`:

```python
SYSTEM_PROMPT = f"""Ты {config.BOT_NAME} - [твое описание персоны]
...
"""
```

### Добавление новых типов ответов

В `responder.py` добавь новый метод `_send_custom()` и обнови `parse_response()`.

### Настройка памяти

В `config.py` измени:
```python
SHORT_MEMORY_LIMIT = 50  # Увеличить краткосрочную память
CONTEXT_MESSAGES_COUNT = 30  # Больше контекста для DeepSeek
```

### Изменение параметров DeepSeek

В `config.py`:
```python
DEEPSEEK_MAX_TOKENS = 200  # Больше максимальная длина ответа
DEEPSEEK_TEMPERATURE = 0.5  # Меньше — детерминированнее, больше — креативнее
```

## Лицензия

MIT License — используй свободно в своих проектах

## Контрибьютинг

Улучшения и баг-репорты приветствуются! 

Создай issue или pull request если нашел ошибку или есть идеи.

---

**Начни разговор с ботом! 🚀**
