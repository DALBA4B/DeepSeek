# Деплой DeepSeek Telegram Bot 🚀

## Бесплатные варианты хостинга

### 1. Railway (Рекомендуется) ⭐

**Бесплатный тир:** $5 кредитов/месяц (достаточно для бота 24/7)

#### Шаги:

1. **Создай аккаунт** на [railway.app](https://railway.app)

2. **Подключи GitHub репозиторий:**
   - New Project → Deploy from GitHub repo
   - Выбери репозиторий с ботом

3. **Настрой переменные окружения:**
   - Перейди в Settings → Variables
   - Добавь все переменные из `.env.example`:
     ```
     TELEGRAM_TOKEN=...
     DEEPSEEK_API_KEY=...
     GIPHY_API_KEY=...
     FIREBASE_CRED_PATH=firebase-creds.json
     GEMINI_API_KEY=...
     BOT_NAME=...
     ```

4. **Загрузи Firebase credentials:**
   - Создай файл `firebase-creds.json` в корне проекта
   - Или используй переменную окружения с JSON содержимым

5. **Деплой автоматический** при каждом push в main

#### Особенности Railway:
- ✅ Автоматический деплой из GitHub
- ✅ Логи в реальном времени
- ✅ Автоматический рестарт при падении
- ✅ $5 бесплатных кредитов/месяц
- ⚠️ После исчерпания кредитов бот остановится

---

### 2. Render

**Бесплатный тир:** 750 часов/месяц (достаточно для одного бота)

#### Шаги:

1. **Создай аккаунт** на [render.com](https://render.com)

2. **Создай новый Background Worker:**
   - New → Background Worker
   - Подключи GitHub репозиторий

3. **Настройки:**
   - Environment: Python 3
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python main.py`

4. **Переменные окружения:**
   - Environment → Add Environment Variable
   - Добавь все переменные

5. **Деплой** нажми "Create Background Worker"

#### Особенности Render:
- ✅ 750 бесплатных часов/месяц
- ✅ Автоматический деплой
- ⚠️ Бесплатные сервисы "засыпают" после 15 минут неактивности
- ⚠️ Для worker это не проблема, но может быть задержка при старте

---

### 3. Fly.io

**Бесплатный тир:** 3 shared-cpu-1x VMs

#### Шаги:

1. **Установи flyctl:**
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   
   # Linux/Mac
   curl -L https://fly.io/install.sh | sh
   ```

2. **Авторизуйся:**
   ```bash
   fly auth login
   ```

3. **Создай приложение:**
   ```bash
   fly launch
   ```

4. **Настрой секреты:**
   ```bash
   fly secrets set TELEGRAM_TOKEN=...
   fly secrets set DEEPSEEK_API_KEY=...
   fly secrets set GIPHY_API_KEY=...
   fly secrets set GEMINI_API_KEY=...
   fly secrets set BOT_NAME=...
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

---

### 4. Google Cloud Run (Сложнее, но мощнее)

**Бесплатный тир:** 2 миллиона запросов/месяц

⚠️ **Не рекомендуется для Telegram ботов** - Cloud Run предназначен для HTTP сервисов, а не для long-polling ботов.

Если хочешь использовать GCP, лучше выбери **Compute Engine** с бесплатным e2-micro инстансом.

---

## Firebase Credentials на хостинге

### Вариант 1: Файл в репозитории (НЕ рекомендуется)

Добавь `firebase-creds.json` в `.gitignore` и загружай вручную.

### Вариант 2: Переменная окружения (Рекомендуется)

1. Закодируй JSON в base64:
   ```bash
   base64 -w 0 firebase-creds.json
   ```

2. Добавь как переменную `FIREBASE_CREDS_BASE64`

3. Модифицируй `config.py` для декодирования:
   ```python
   import base64
   import json
   import tempfile
   
   firebase_creds_base64 = os.getenv("FIREBASE_CREDS_BASE64")
   if firebase_creds_base64:
       creds_json = base64.b64decode(firebase_creds_base64).decode()
       with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False) as f:
           f.write(creds_json)
           FIREBASE_CRED_PATH = f.name
   ```

---

## Мониторинг

### Логи

- **Railway:** Dashboard → Deployments → View Logs
- **Render:** Dashboard → Logs
- **Fly.io:** `fly logs`

### Рестарт

- **Railway:** Dashboard → Deployments → Redeploy
- **Render:** Dashboard → Manual Deploy
- **Fly.io:** `fly apps restart`

---

## Рекомендация

Для твоего бота я рекомендую **Railway**:

1. ✅ Простой деплой из GitHub
2. ✅ $5 бесплатных кредитов достаточно для 24/7 работы
3. ✅ Хорошие логи и мониторинг
4. ✅ Автоматический рестарт при падении
5. ✅ Поддержка переменных окружения

Если Railway закончит кредиты, можно переключиться на **Render** или **Fly.io**.
